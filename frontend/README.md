# URL Shortener — Frontend

React + Vite + Recharts dashboard.

```bash
npm install
npm run dev
```

Opens at http://localhost:5173 and proxies `/api/*` to the backend at `http://localhost:4000`.

Start the backend first (see `../backend/README.md`).
